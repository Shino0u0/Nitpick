name: Nitpick
packages:
  FluidAudio:
    url: https://github.com/FluidInference/FluidAudio
    exactVersion: 0.15.5
options:
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "15.0"
settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    CODE_SIGN_STYLE: Automatic
    GENERATE_INFOPLIST_FILE: true
    DEAD_CODE_STRIPPING: true
targets:
  Nitpick:
    type: application
    platform: macOS
    productName: Vortext
    sources:
      - Nitpick
    dependencies:
      - package: FluidAudio
    entitlements:
      path: Nitpick/Resources/Nitpick.entitlements
      properties:
        com.apple.security.device.audio-input: true
    info:
      path: Nitpick/Resources/Info.plist
      properties:
        CFBundleDisplayName: Vortext
        LSUIElement: true
        LSApplicationCategoryType: public.app-category.productivity
        NSMicrophoneUsageDescription: Nitpick records audio only while you dictate or ask, and transcribes it on this Mac.
        NSHumanReadableCopyright: "Copyright Nitpick contributors. GPLv3."
    settings:
      base:
        # Bundle id stays Nitpick-stable: Keychain items and TCC permission
        # grants (mic, accessibility) are keyed to it. Only the product name
        # and display name are Vortext.
        PRODUCT_BUNDLE_IDENTIFIER: io.github.shino0u0.Nitpick
        PRODUCT_NAME: Vortext
        PRODUCT_MODULE_NAME: Nitpick
        ENABLE_HARDENED_RUNTIME: true
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
  NitpickTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - NitpickTests
    dependencies:
      - target: Nitpick
  NitpickUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - NitpickUITests
    dependencies:
      - target: Nitpick
schemes:
  Nitpick:
    build:
      targets:
        Nitpick: all
    test:
      gatherCoverageData: true
      targets:
        - NitpickTests
        - NitpickUITests
